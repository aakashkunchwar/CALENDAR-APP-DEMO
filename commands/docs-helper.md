Scan all files in this project and generate (or overwrite) README.md.

The README must be beginner-friendly and include:

1. **Project title & one-line description**
2. **Features** — bullet list of everything the app can do
3. **Getting Started** — step-by-step setup (clone, open in browser, no install needed)
4. **How to Use** — example usage walkthrough (adding an event, editing, deleting)
5. **Project Structure** — file tree with a one-line description of each file
6. **Tech Stack** — simple table (HTML / CSS / JS / localStorage)
7. **Accessibility** — note that the app meets WCAG 2.1 AA

Keep language simple. Avoid jargon. Assume the reader has never used a calendar app or opened dev tools.
